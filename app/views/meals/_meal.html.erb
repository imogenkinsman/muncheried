<div class="meal">
  <% if meal.category == "Entree" %>
    <%= image_tag "plates.svg", class: side %>
  <% else %>
    <%= image_tag "plate.svg", class: side %>
  <% end %>
  <%= link_to meal.name, meal.url, class: 'name' %>
  <span>$<%= meal.price %></span>
</div>